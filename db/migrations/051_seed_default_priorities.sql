-- Migration 051: Seed de Prioridades Padrão  
-- Por enquanto vamos criar apenas a estrutura, as prioridades serão inseridas via código quando necessário

-- Comentário explicativo sobre o sistema de fallback
-- O sistema usará prioridades padrão (low, medium, high, critical) quando não houver prioridades customizadas
-- configuradas para um departamento específico. 